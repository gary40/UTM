# 專案慣例

## 語言

本專案**預設一律使用繁體中文（台灣，zh-Hant-TW）**：

- 所有回覆、說明、討論預設以繁體中文（台灣）進行。
- 介面文字、錯誤訊息、README 與程式註解一律使用繁體中文（台灣用語）。
- Commit 訊息與 PR 標題、內文使用繁體中文（台灣）。
- 語言標記統一使用 `zh-Hant-TW`（HTML `lang` 屬性、HTTP `Content-Language` 標頭）。
- 技術名詞（UTM、SQLite、Express 等）與程式識別字保留英文原文即可。

## 專案簡介

UTM 連結產生器：前台填寫表單 → 後端驗證正規化 → 寫入 SQLite 存檔。
詳見 `README.md`。
